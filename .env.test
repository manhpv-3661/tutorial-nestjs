NODE_ENV=test
PORT=3000

# Database - separate from the dev DB in .env so e2e runs never touch dev data
DB_HOST=localhost
DB_PORT=5433
DB_USERNAME=nestjs
DB_PASSWORD=nestjs
DB_NAME=nestjs_tutorial_test

# Redis
REDIS_HOST=localhost
REDIS_PORT=6380

# JWT
JWT_SECRET=change-me
JWT_EXPIRES_IN=86400
